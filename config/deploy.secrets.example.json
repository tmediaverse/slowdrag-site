{
    "deployment":  {
                       "password":  "SET-LOCALLY-ONLY"
                   },
    "notes":  [
                  "Copy this file to deploy.secrets.json and replace the placeholder password.",
                  "Keep deploy.secrets.json out of Git. The project .gitignore is configured for it.",
                  "You can also set the environment variable TMC_SLOWDRAG_SITE_HOSTINGER_FTP_PASSWORD instead of creating deploy.secrets.json."
              ]
}
