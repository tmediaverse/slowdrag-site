# Workspace Instructions

Read [archetype.md](d:\Edit Files\Slowdrag website building\archetype.md), [chatnotes.md](d:\Edit Files\Slowdrag website building\chatnotes.md), and [WAKEUP.md](d:\Edit Files\Slowdrag website building\WAKEUP.md) at the start of every new session in this workspace.

## Expectations
- Treat `archetype.md` as the canonical project memory.
- Treat `chatnotes.md` as the canonical collaboration-memory and interaction-pattern log.
- Treat `WAKEUP.md` as the canonical wake-up trigger protocol for fast reorientation.
- Refresh it whenever meaningful facts, decisions, or priorities change.
- Refresh `chatnotes.md` whenever recurring preferences, communication patterns, or feedback tendencies become clearer.
- Refresh `WAKEUP.md` whenever the preferred wake-up ritual or boot behavior changes.
- Keep `Current Snapshot`, `Open Questions`, `Session Log`, and `Change Log` current.
- Keep `chatnotes.md` current with concise session summaries and durable user-preference notes.
- Summarize decisions and verified findings clearly.
- Do not store private chain-of-thought; store concise working notes and decision summaries instead.

## Wake Up Trigger
If the user types `WAKE UP`:
- reload `AGENTS.md`, `WAKEUP.md`, `archetype.md`, and `chatnotes.md`
- reconstruct the user's working style and current project context from those files
- re-scan relevant active files when needed
- respond with a concise wake-up summary before continuing with new execution

## Update Rule
When new information supersedes older information:
- update the current section directly
- append a short note to the `Change Log`
- preserve historical session context in `Session Log`
